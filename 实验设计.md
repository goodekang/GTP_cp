# 论文设计方案：基于时空基座模型的基因-表型因果Transformer
# (Gene-to-Phenotype Causal Transformer: Deciphering Disease Progression via Spatial-Temporal Foundation Models)

**版本：** V3.0
**日期：** 2025-12-15
**核心目标：** 超越单纯的“多模态预测”，实现“机制解析”与“虚拟临床干预”。

---

## 1. 摘要与核心定位 (Abstract & Positioning)

### 1.1 背景 (Background)
当前的AI医学模型多停留在“相关性预测”层面，缺乏对疾病微观机制（细胞互作、空间异质性）到宏观表型（生存期、耐药性）的因果解释。尽管 **Nicheformer (2025)** 解决了空间重构问题，**CASCAT (2025)** 引入了细胞轨迹因果推断，但目前仍缺乏一个统一框架能将**微观空间拓扑**与**宏观临床结局**直接关联，并支持**虚拟干预 (In Silico Perturbation)**。

### 1.2 核心假设 (Hypothesis)
疾病的临床转归（如癌症复发）是由**特定空间邻域内的关键细胞互作网络结构**因果驱动的，而非单一基因表达。通过构建跨尺度的因果图神经网络，我们可以捕捉这种拓扑结构，并在计算机中模拟“阻断”特定互作后的临床获益。

### 1.3 创新点 (Novelty Highlights)
1.  **架构创新：** 从传统的 CNN/GNN 升级为 **Hierarchical Causal Graph Transformer**，解决跨尺度（基因-细胞-组织）信息流整合。
2.  **方法创新：** 引入 **In Silico Clinical Trials (虚拟临床试验)** 模块，不仅预测风险，更能筛选药物靶点。
3.  **数据策略：** 采用 **Mosaic Integration (马赛克式集成)** 策略，利用小样本高质量空间数据（HTAN）训练基座，在大样本临床数据（TCGA）上实现零样本/少样本迁移。

---

## 2. 技术路线与模型架构 (Methodology)

### 2.1 模型名称：Causal-STFM (Causal Spatial-Temporal Foundation Model)

### 2.2 总体架构 (Architecture Overview)
模型由三个关键模块组成：

#### A. 跨尺度图构建器 (The Multi-Scale Graph Builder)
* **Level 0 (Gene):** 细胞内的基因调控网络 (GRN)，基于 Transformer 提取 Gene Embeddings。
* **Level 1 (Cell):** 细胞作为节点。节点特征来自 scRNA-seq 预训练模型 (如 scGPT/Geneformer)。
* **Level 2 (Tissue/Patch):** 基于病理图像 (WSI) 分割出的超像素 (Superpixels) 或 Patch。
* **构建策略：** 采用 **Hierarchical Graph (分层图)** 结构，避免全连接导致的显存爆炸。

#### B. 因果掩码注意力机制 (Causal Masked Attention)
* 不同于标准的 Self-Attention，我们引入 **Knowledge-Guided Mask**。
* 利用 PPI 网络和 Reactome 通路库作为先验约束，限制注意力权重的分布，确保模型学到的“关注点”符合生物物理学定律，而非虚假相关。

#### C. 虚拟扰动模块 (The Perturbation Module)
* **功能：** 在推理阶段，人为将某个节点（如 M2型巨噬细胞）的特征置零，或切断某条边（如 PD-1/PD-L1 互作）。
* **输出：** 计算 $\Delta Risk = Risk_{original} - Risk_{perturbed}$，量化该互作对患者生存的**因果贡献度**。

---

## 3. 数据与训练策略 (Data & Training Strategy)

### 3.1 数据来源 (Data Sources)
* **基座训练 (Pre-training):** Human Cell Atlas (HCA), DISCO (单细胞数据, >10M cells)。
* **桥接学习 (Bridge):** HTAN, 10x Genomics Datasets (配对的 scRNA + Spatial + H&E)。
* **下游应用 (Downstream):** TCGA, ICGC (大规模 WSI + 临床生存数据)。

### 3.2 训练流程 (Three-Stage Training)
1.  **Stage 1: Masked Tissue Modeling (MTM)**
    * *任务：* 遮挡空间转录组中的部分区域，利用周围环境预测缺失区域的基因表达。
    * *目的：* 学习“组织形态”与“基因表达”的通用映射规则。
2.  **Stage 2: Domain Adversarial Adaptation (域自适应)**
    * *任务：* 引入对抗判别器，混淆 HTAN (高质量空间数据) 与 TCGA (普通病理数据) 的特征分布。
    * *目的：* 解决 **Domain Shift**，确保模型在只有 H&E 的 TCGA 数据上也能“脑补”出微观特征。
3.  **Stage 3: Survival Fine-tuning (生存微调)**
    * *任务：* 使用 Cox Loss 或 DeepSurv 目标函数，在 TCGA 队列上进行端到端微调。

---

## 4. 验证与实验设计 (Validation & Experiments)

### 4.1 核心实验 (Main Results)
* **性能对比：** 比较 Causal-STFM 与 SOTA 模型 (如 MCAT, Porpoise, Nicheformer) 在 5 年生存率预测上的 C-index。
* **跨队列验证：** 在完全独立的外部医院队列中测试模型的鲁棒性。

### 4.2 机制解析 (Interpretability)
* **Case Study:** 展示模型在“高风险”患者中识别出的**空间基序 (Spatial Motif)**（例如：成纤维细胞包围 CD8+ T 细胞形成的免疫排斥屏障）。
* **虚拟筛选验证:** 模型预测阻断 Gene X 有效 -> 引用文献证明 Gene X 抑制剂在相关癌症中的临床效果。

### 4.3 必要性验证 (Ablation Studies)
* **No-Spatial:** 去掉空间位置信息，仅用 scRNA 预测 -> 证明空间拓扑的重要性。
* **No-Causal:** 去掉因果掩码约束 -> 证明因果引导能提高泛化性。

---

## 5. 可行性评估与风险管理 (Feasibility & Risk)

### 5.1 潜在风险 (Risk)
* **计算瓶颈：** WSI 构建的图过于庞大。
    * *对策：* 采用 Patch-based Hierarchical Graph，将节点数控制在 <5000，适配 A100/4090 显存。
* **域漂移 (Domain Shift):** TCGA 图像质量参差不齐。
    * *对策：* 必须实施强力的 Color Normalization (颜色归一化) 和 Adversarial Training (对抗训练)。

### 5.2 软硬件需求
* **Hardware:** NVIDIA A100 (80GB) 或 RTX 4090 x 2。
* **Software:** PyTorch, PyG (PyTorch Geometric), Scanpy, OpenSlide, HuggingFace Transformers.

---

## 6. 下一步行动计划 (Action Plan)

1.  **Week 1:** 下载 HTAN (CRC/Breast) 数据，跑通 "H&E -> Spatial Expression" 的预处理与可视化流程。
2.  **Week 2:** 构建简单的 Graph Transformer demo，测试显存占用。
3.  **Week 3:** 完成 TCGA 数据清洗与标签对齐。

---

## 7. Nature 子刊“一次到位”发表版实验计划（全公开数据集，可复现）

> 原则：所有结论都必须由**公开数据 + 固定协议**直接产出；主文只讲 3 条贡献，其余全部放 Extended Data/Methods；从 Day-1 就按审稿标准做外部验证、统计与消融。

### 7.1 论文要打穿的 3 条核心 Claim（只保留 3 条）
1. **性能（临床有效）**：在公开 WSI 生存预测基准上显著优于强基线，并在独立外部公开队列复现（C-index + KM/HR）。
2. **机制（可解释）**：模型发现的空间细胞社区/空间模体与生存显著相关，且在外部队列保持方向一致。
3. **干预（可检验的反事实）**：基于知识约束的扰动比随机扰动更能产生结构化的风险变化，并与外部证据（LR/CRISPR/药敏数据库）一致。

### 7.2 数据集选择（公开、可下载、可复现；与 3-stage 训练对齐）

#### A) Downstream 主任务（Stage 3：Survival Fine-tuning）
- **TCGA WSI + 临床生存**：作为主训练/内部验证来源（按 patient 切分，避免泄漏）。
- **癌种选择（主文聚焦 + 补充泛化）**
  - **主文聚焦 1 个癌种**：优先 BRCA 或 LUAD（样本量较稳、外部 WSI 队列更容易找）。
  - **Extended Data 泛化**：再做 6–8 癌种森林图（同一协议、同一基线列表）。

#### B) 外部验证（必须公开且独立）
- **CPTAC（通过 TCIA 的 WSI + 临床）**：作为外部独立队列（与 TCGA 分布不同，最能说服审稿人）。
  - 目标：同一模型（不再调参）在外部队列给出 C-index + KM/HR。

#### C) Bridge/Pre-train（Stage 1/2：Spatial 相关）
- **HTAN/10x 公开的 paired H&E + Spatial（Visium/Xenium）**：用于学习“形态→空间信号”的桥接表征（不需要临床结局）。
  - 目标：给出空间重构指标（相关性/MAE）与可视化（与 Figure 2c、Figure 3c 对齐）。

> 注：若 HTAN 获取受控或下载成本高，优先用 10x 官方公开数据集完成桥接阶段；HTAN 作为补充/增强（写清楚来源与版本）。

### 7.3 统一的“冻结协议”（避免后期反复补实验）

#### 7.3.1 标签与纳排（写进 Methods，并冻结）
- **终点**：优先 OS（最通用、争议最少）；单位统一（月）。
- **删失**：明确 event=1/0 的定义与随访起点。
- **纳排**：缺随访/极短随访/关键信息缺失的处理规则固定。

#### 7.3.2 切分与验证（必须 patient-level）
- **内部**：Train/Val/Test（patient-level，固定随机种子，固定一次切分并存档）。
- **外部**：CPTAC 全量作为 external test（不参与任何调参）。
- **避免泄漏**：同一患者多张 slide/多个 block 必须在同一 split。

#### 7.3.3 基线与消融（主文必须有，且从 Day-1 固定）
- **强基线（WSI survival 常见体系）**：MIL/Transformer 系列 + 多模态对照（你们方法输入相同/可比）。
- **消融**：
  - **No-Spatial**（去掉空间/邻域结构，只保留 token/MIL）
  - **No-Causal**（去掉知识约束 mask）
  - **No-Bridge**（不做 bridge/预训练，直接在 TCGA 训练）

#### 7.3.4 统计与汇报（严格对齐 Figure 3/ED）
- **主指标**：C-index（bootstrap 95% CI）。
- **KM 图**：阈值（median/tertile）固定；报告 HR(95%CI)、log-rank P、N。
- **多癌种**：森林图统一基准线与排序；报告每癌种 N。

### 7.4 “虚拟干预”必须产出的审稿人证据包（对应 Figure 5 + ED）
- **Biologically informed perturbation vs Random perturbation**：随机扰动对照必须做（ED）。
- **Null model test**：打乱图结构/打乱 mask 的零模型分布（ED）。
- **Top target 外部证据**：LR 数据库/CRISPR/药敏/GDSC 等至少一种可公开复核的证据源（Methods 写清）。

### 7.5 一次性出稿所需交付物清单（从实验直接产出写作材料）
- **Data registry**：每个队列下载链接/版本/日期/过滤后 N（做成表格，放 ED）。
- **ID mapping 表**：patient_id ↔ slide_id ↔ clinical_id（可复现对齐）。
- **Config + 环境锁**：保存训练配置与 conda 锁文件（env.lock.yml）。
- **Figure-ready 输出**：所有主图/补图对应的 csv（指标、CI、HR、Ablation、效率）+ 可复现脚本。
