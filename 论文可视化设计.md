## 论文整体可视化设计方案（优化版）

> 目标：把 `实验设计.md` 的主线（基座迁移→因果约束→虚拟干预→临床获益）翻译成**一致、克制、可复用**的视觉语言；让读者在 Figure 1 就建立正确心智模型，在 Figure 3–5 被结果“打穿”。

---

## 0. 全局视觉系统（Design System）

### 0.1 统一语义配色（全论文只用“少量固定语义色”）

- **Source / 空间高质队列（HTAN/10x paired）**：蓝绿系（建议偏青蓝，代表“空间知识/微观真实”）
- **Target / 大规模临床队列（TCGA/ICGC）**：橙/金系（代表“临床结局/宏观表型”）
- **Model / 表示模型主干（Causal-STFM）**：深灰或靛蓝（中性、稳重）
- **Causal constraint / Knowledge-guided mask（PPI/Reactome）**：紫色（代表“因果/先验”）
- **Intervention / Perturbation（cut / zero-out）**：红色（只在“干预/反事实”出现）
- **Baseline methods**：统一灰阶；避免每个 baseline 一种彩虹色

> 规则：颜色只表达“语义”，不表达“装饰”。同一语义在所有 Figure/ED 图中必须一致。

### 0.2 图形语法（让读者形成“看图的语法”）

- **数据形态**
  - 队列/数据源：圆柱/数据库图标（HTAN/TCGA 用语义色描边）
  - 图/网络：节点-边（节点形状区分 level：Gene=小点、Cell=圆、Patch=方）
  - 病理图像：矩形 tile + 微小网格纹理（避免真实 H&E 占比过大抢视线）
- **流程方向**
  - 默认左→右（Inputs → Builder → Backbone → Heads → Outputs）
  - 只有“干预/反事实”允许出现回路（Original → Perturb → Re-score）
- **面板标注**
  - a,b,c… 固定左上角；同一字号/同一描边框
  - 每个 panel 只允许 1 句 take-home（短标题），其余放 caption

### 0.3 字体、线条、留白（保证出版级一致性）

- **字体**：统一无衬线（Arial/Helvetica/Source Sans）；数学公式用期刊默认或 LaTeX
- **字号建议（以单栏 85mm/双栏 180mm 输出为参考）**
  - 面板标题：8–9 pt
  - 坐标轴/图例：7–8 pt
  - 注释：6–7 pt（尽量少）
- **线宽**
  - 轴线：0.5–0.75 pt
  - 主流程箭头：1.0–1.5 pt
  - 次要连线：0.5 pt（灰）
- **留白**
  - “概念/架构”图：留白 > 密度
  - “结果”图：密度 > 留白（但要严格对齐、统一图例）

### 0.4 导出与可复现

- 概念/架构：优先矢量（PDF/SVG），字体最终转曲
- 热图/WSI：位图 300–600 dpi（看期刊要求），色条一致
- 结果图主题：建议固定一套 ggplot/matplotlib theme（统一字体、网格线、legend）

### 0.5 “必须写清楚”的出版要素清单

- **生存相关图（KM/HR）**：N、随访时间单位、HR 与 95% CI、log-rank P、分组阈值（median/tertile/预设 cut）
- **泛癌/多队列结果**：每个癌种样本量 N、外部验证队列来源（医院/公开库）、是否做分层/校正
- **消融/对比**：baseline 的统一命名与排序（所有图一致），以及是否同一数据切分
- **效率图**：GPU 型号、显存、batch size、图节点规模、吞吐定义（patch/s 或 WSI/min）

---

## 1. 全论文 Figure Map（叙事节奏）

- **Figure 1（概念总览）**：回答“你在解决什么难题 + 你提出了什么统一框架”
- **Figure 2（技术架构）**：回答“为什么能工作（模块如何连接 + 因果约束如何进入模型）”
- **Figure 3（基准结果）**：回答“你真的更准、更稳、更省”
- **Figure 4（机制解释）**：回答“模型学到了可解释机制（空间模体/细胞互作）”
- **Figure 5（虚拟临床试验）**：回答“可转化：从解释到干预与靶点排序”

可选（视投稿期刊）：**Graphical Abstract**（一页/一图，把 Figure 1b 缩写成更“海报式”的版本）

---

## 2. Figure 1：概念全景与生物学逻辑流（The Conceptual Framework）

### Take-home（放在图的中央或 caption 首句）
**跨队列“马赛克式集成”把空间微观机制迁移到大规模临床 WSI，从相关性预测升级到可干预的因果解释。**

### 版式（建议：Full-width，强中心叙事）
- **中间大 panel（b）占 45–55% 宽度**，两侧辅助 panel（a/c/d/e）围绕它“提供上下文”
- 阅读顺序：a（问题）→ b（方案）→ c（表征）→ d（干预）→ e（临床价值）

#### Panel a：数据悖论（The Data Paradox）
- **目标**：一句话让读者理解“为什么必须做 bridge/transfer”
- **图形**：左右两块“数据岛”+ 断裂桥
  - 左：TCGA（橙）= 大样本 + survival
  - 右：HTAN/paired spatial（蓝绿）= 小样本 + spatial truth
- **标注要点**：N 的数量级差（\(10^3\) vs \(10^1\)）+ 缺失字段（spatial / outcome）

#### Panel b：总体工作流（Overall Workflow，核心）
- **结构**：Inputs → Pre-train/Bridge → Transfer → Fine-tune → Outputs
- **关键视觉**：把“Spatial Knowledge”画成可迁移的“发光约束/先验”（紫+蓝绿），注入到 TCGA 的 WSI pipeline
- **避免**：堆太多模块小框；只保留 5–7 个核心节点

#### Panel c：跨尺度图谱（Multi-scale Graph）
- **图形**：三层金字塔/分层图（Gene→Cell→Patch）
- **强调**：Spatial Neighbor 边是“物理约束”（用灰细线+距离阈值标注）

#### Panel d：虚拟干预（In Silico Intervention）
- **图形**：Original graph → Cut edge/zero node（红）→ Re-score（\(\Delta Risk\)）
- **一句话**：从注意力“看哪里”到反事实“改哪里会变”

#### Panel e：临床落地（Clinical Utility）
- **只放两个 icon + 1 句量化指标**
  - 预后：KM 曲线分离更明显（示意即可）
  - 靶点：Top-1/Top-10 列表收敛（从“噪声”到“可行动”）

---

## 3. Figure 2：技术架构与数学原理（The Technical Architecture）

### 总原则：不要做“六宫格独立拼贴”，要做“主干 + 局部放大”

### 推荐版式（Full-width 或双栏宽）
- 左：**Graph Builder（数据→异构分层图）**
- 中：**Causal Masked Graph Transformer（核心创新，放大框）**
- 右：**训练三阶段（MTM → Domain Adv → Survival）+ Heads**

#### Panel a：异构/分层图构建器（Graph Builder）
- WSI → patch/superpixel → patch graph
- paired spatial/scRNA → cell graph（节点特征来自预训练嵌入）
- gene level（可用“可选层”标注，避免画到 \(10^9\) 细节）

#### Panel b：因果掩码注意力（核心）
- **画法**：标准 Attention block + Mask Matrix \(M\) 作为“外置门控”
- **重点**：\(M\) 来自 PPI/Reactome（紫色网络形状叠在矩阵上）
- **一句话**：Biologically-constrained attention reduces spurious correlations

#### Panel c：MTM（Masked Tissue Modeling）
- 一张 patch 网格“挖空”→ 重建基因表达（色块/heatmap）

#### Panel d：域对抗（Domain Adversarial）
- Generator/Discriminator + GRL 符号
- 不需要真实 loss 曲线，除非你确实要在主图展示（可放 ED）

#### Panel e：反事实风险（Perturbation Algorithm）
- 伪代码只保留 6–8 行（输入图、切边、前向、\(\Delta Risk\)）
- 旁边给 1 个 toy graph 例子（切一条边，risk 下降）

#### Panel f：输出头（Task Heads）
- Survival head（Cox/DeepSurv）
- Spatial reconstruction head（辅助任务）
- 可加 calibration/uncertainty（若有）

---

## 4. Figure 3：综合基准测试（Comprehensive Benchmarking）

### 版式建议：让“最有说服力”的 panel 占最大面积
- **b（外部验证 KM）必须最大**（建议占一整行或半页）
- a/e（C-index + Ablation）作为“定量主证据”
- c/d/f 作为“你不是只会一个指标”的辅助证据

#### Panel a：Pan-cancer C-index（条形/点图）
- 推荐点图（均值+95% CI）比纯条形更“期刊风”
- baseline 全灰，你们方法高亮（深色）

#### Panel b：External cohort KM
- 标注：N、HR、log-rank P、时间单位
- 颜色：高/低风险两色固定，跨所有 KM 图一致

#### Panel c：Spatial expression correlation
- 只挑代表性基因/区域：避免 30 个子图塞满

#### Panel d：Platform robustness（Visium vs Xenium）
- 用 paired scatter + Bland–Altman（二选一）即可，别两者都放主图

#### Panel e：Ablation（No-Spatial / No-Causal / No-Bridge）
- 建议做“性能下降的瀑布/棒棒糖图”（更直观）

#### Panel f：效率（GPU mem vs #nodes / throughput）
- 明确硬件设置与 batch size，否则读者不信

---

## 5. Figure 4：空间机制解析与生物学发现（Spatial Mechanisms & Discovery）

### 目标：让读者看到“可复核的生物机制”，而不是“漂亮热图”

#### Panel a：WSI + heatmap overlay
- WSI 只做背景；heatmap 透明度控制；给 1–2 个 ROI 框

#### Panel b：ROI 放大（细胞社区）
- 展示细胞类型分布（点图）+ 邻域边（灰）+ 高风险社区边界（粗线）

#### Panel c：Spatial Motif 拓扑
- 画成小网络：节点=细胞类型，边=互作（可标注 ligand–receptor）
- 用红色只表示“关键边/关键节点”，其余灰

#### Panel d：Motif enrichment vs survival
- 箱线/小提琴 + 统计检验；别忘了样本量

#### Panel e：Gene–Morphology 关联
- 推荐 2×3 小矩阵：形态指标（核大小/密度/异型性…）× 关键基因
- 只展示 top few（不然像“相关性大全”）

---

## 6. Figure 5：虚拟临床试验与药物靶点（Virtual Clinical Trials）

### 版式建议：a（全局排序）+ b（单靶点深挖）双主证据

#### Panel a：In silico screening waterfall
- y 轴：\(\Delta Risk\) 或 predicted benefit
- x 轴：Top-100 interactions（按效应排序）
- 标注：Top-1/Top-5 名称（其余不标）

#### Panel b：Top-1 靶点的 counterfactual survival
- 同一批病人：Original vs Perturbed（红）
- 必须写清“干预强度”（cut edge / inhibition %）

#### Panel c：UMAP trajectory（冷→热）
- 只要 1 张清晰图：箭头方向 + 处理前后密度变化
- 避免把“多组学+轨迹+箭头+legend”全塞进来

#### Panel d：外部证据
- 两栏结构：左（CRISPR/药敏/数据库 overlap），右（关键文献结论一句话）
- 以“可验证性”为核心，而不是“引用堆叠”

---

## 7. Extended Data

### ED Fig. 1：QC 与图构建细节
- QC 小提琴：HTAN vs TCGA 两列对齐
- Neighborhood sensitivity：把“最佳阈值”画成竖线并标注
- Hierarchical compression：用对数轴或数量级箭头，避免长数字

### ED Fig. 2：超参数与骨干消融
- Backbone comparison：箱线图统一排序（最差→最好）
- Head×Layer 热图：配色固定（低 loss=深色）
- Masking ratio：给最佳区间阴影（30–50%）

### ED Fig. 3：解释稳定性
- attention entropy：密度图+均值线
- random perturbation：bar + 显著性标记

### ED Fig. 4：Pan-cancer & subgroup
- forest plot：统一基准线（HR=1 或 \(\Delta\)=0）
- demographics：训练/验证同色系不同明度

### ED Fig. 5：虚拟筛选统计学验证
- Null model：直方图 + 红线（真实值）+ p 值
- overlap：Venn（慎用，信息密度低）或 UpSet（更严谨）
- dose-response：S 型曲线标出 EC50（若可定义）